{
  "name": "@smarterlabs/react-events",
  "private": false,
  "version": "0.0.0",
  "author": "Kennedy Rose <kennedyianrose@gmail.com>",
  "main": "use-events.js",
  "scripts": {
    "build": "yarn run clean && babel src --out-dir dist",
    "clean": "del dist",
    "dev": "babel src --watch --out-dir dist",
    "test": "yarn run build && jest"
  },
  "peerDependencies": {
    "react": "*"
  },
  "devDependencies": {},
  "dependencies": {}
}
