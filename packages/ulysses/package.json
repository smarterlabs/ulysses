{
  "name": "@smarterlabs/ulysses",
  "private": false,
  "version": "0.0.1",
  "author": "https://smarterlabs.com",
  "main": "dist/index.js",
  "scripts": {
    "build": "yarn run clean && babel src --out-dir dist",
    "clean": "del dist",
    "dev": "babel src --watch --out-dir dist",
    "test": "yarn run build && jest"
  },
  "peerDependencies": {
    "react": "*"
  },
  "devDependencies": {
    "@babel/compat-data": "7.8.0"
  }
}
