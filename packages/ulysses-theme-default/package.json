{
  "name": "@smarterlabs/ulysses-theme-default",
  "private": false,
  "version": "0.0.2",
  "author": "https://smarterlabs.com",
  "main": "dist/index.js",
  "scripts": {
    "build": "yarn run clean && babel src --out-dir dist",
    "clean": "del dist",
    "dev": "babel src --watch --out-dir dist",
    "test": "yarn run build && jest"
  },
  "peerDependencies": {
    "@smarterlabs/ulysses": "*",
    "react": "*"
  }
}
