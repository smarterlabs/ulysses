{
  "name": "@smarterlabs/ulysses-js",
  "private": false,
  "version": "0.0.0",
  "author": "Kennedy Rose <kennedyianrose@gmail.com>",
  "main": "dist/index.js",
  "scripts": {
    "build": "yarn run clean && babel src --out-dir dist",
    "clean": "del dist",
    "dev": "yarn run build && babel src --watch --out-dir dist",
    "test": "yarn run build && jest"
  },
  "devDependencies": {},
  "dependencies": {
    "@babel/runtime": "^7.8.3"
  }
}
