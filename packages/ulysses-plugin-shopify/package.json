{
  "name": "@smarterlabs/ulysses-plugin-shopify",
  "private": false,
  "version": "0.0.0",
  "author": "https://smarterlabs.com",
  "main": "index.js",
  "scripts": {
    "build": "yarn run clean && babel src --out-dir dist",
    "clean": "del dist",
    "dev": "babel src --watch --out-dir dist",
    "test": "yarn run build && jest"
  },
  "peerDependencies": {
    "@smarterlabs/ulysses-plugin-shopify": "*",
    "react": "*"
  },
  "dependencies": {
    "deepmerge": "^4.2.2"
  }
}
